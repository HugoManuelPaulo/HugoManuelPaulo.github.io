const menu=document.getElementById("menu"),nav=document.getElementById("navlinks");
if(menu&&nav){menu.addEventListener("click",()=>{const open=nav.classList.toggle("open");menu.setAttribute("aria-expanded",String(open));document.body.classList.toggle("menu-open",open)});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");document.body.classList.remove("menu-open");menu.setAttribute("aria-expanded","false")}))}
let current="nl";const lang=document.getElementById("lang");if(lang)lang.addEventListener("click",()=>{current=current==="nl"?"en":"nl";document.documentElement.lang=current;document.querySelectorAll("[data-nl][data-en]").forEach(el=>el.innerHTML=el.dataset[current]);lang.textContent=current==="nl"?"EN":"NL"});
const form=document.getElementById("demo-form");if(form)form.addEventListener("submit",e=>{e.preventDefault();const toast=document.getElementById("toast");if(toast){toast.textContent=current==="nl"?"Demomodus: er zijn geen gegevens verzonden.":"Demo mode: no data has been sent.";toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),3500)}e.target.reset()});
const year=document.getElementById("year");if(year)year.textContent=new Date().getFullYear();
const rateSearch=document.getElementById("rate-search"),rateRows=[...document.querySelectorAll("#rate-body tr")],filterButtons=[...document.querySelectorAll(".filter")];let activeFilter="all";
function filterRates(){if(!rateSearch)return;const q=rateSearch.value.toLowerCase().trim();let shown=0;rateRows.forEach(row=>{const visible=(activeFilter==="all"||row.dataset.cat===activeFilter)&&row.textContent.toLowerCase().includes(q);row.hidden=!visible;if(visible)shown++});const empty=document.getElementById("no-rate");if(empty)empty.style.display=shown?"none":"block"}
if(rateSearch){rateSearch.addEventListener("input",filterRates);filterButtons.forEach(btn=>btn.addEventListener("click",()=>{filterButtons.forEach(b=>b.classList.remove("active"));btn.classList.add("active");activeFilter=btn.dataset.filter;filterRates()}))}
